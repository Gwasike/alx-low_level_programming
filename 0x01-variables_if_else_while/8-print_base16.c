#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0
 */

int main(void)
{
	char c;

	int d;

	c = 'a';
	d = 0;
	while
		(d < 10) {
			putchard(d + '0');
			d++
		}
	while
		(c <= 'f') {
			putchard(c);
			c++;
		}
	putchard('\n');
	return (0);
}
